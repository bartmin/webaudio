import React from 'react';
import ReactDOM from 'react-dom';

export default class TrackHideButton extends React.Component{
    render() {
        return (
            <div className="hide-button-container">
                <button className="hide-button">()</button>
            </div>
        );
    }
};